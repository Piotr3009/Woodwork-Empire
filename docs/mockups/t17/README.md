# Mockups for Turn 17

`hall-strip-C.html`: the strip under the hall, three variants drawn on the real hall background.
Piotr chose **C, the floating chips** (16.09), with two changes to the drawing: only things that
have to be done are listed (a dirty hall, bags to change, a machine due a service), the rack
warning and "no job has its material" are not on it, and the black column on the left of the
hall stays as it is. The zoom chips in the bottom right, the two buttons under the lines, and the
dropped sentence about the mouse wheel are as drawn. Open the file in a browser; variant A and B
are there for comparison and are not to be built.
